<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>License notes</title>
    <style>
      body {
        font-size: medium;
        line-height: 1.5;
      }
      h1 {
        font-size: xxx-large;
      }
      h2 {
        font-size: xx-large;
      }
      h3 {
        font-size: x-large;
        font-weight: normal;
      }
      main {
        margin-block-start: 2%;
        margin-block-end: 2%;
      }
      pre {
        outline: thin inset;
        padding: 1%;
      }
      #licenses-list article + hr {
        visibility: hidden;
        margin-block-start: 1.3%;
        margin-block-end: 1.3%;
      }
      footer {
        border-block-start-style: dashed;
        border-block-start-width: 2px;
        padding: 2vh;
      }
      footer aside::before {
        content: "* "
      }
    </style>
  </head>
  <body>
    <header>
      <h1>License notes</h1>
      <p>This page lists the licenses of the projects used in <cite>gles31_util</cite>.
      <section id="licenses-overview">
        <h2>Overview of licenses:</h2>
        <ul>
          {{#each overview}}
          <li><b>{{name}}</b> - {{count}} crates</li>
          {{/each}}
        </ul>
      </section>
    </header>
    <main id="licenses-list">
      <h2>Licenses retrieved as possible in cargo-about:</h2>
      {{#each licenses}}
      <article>
        <h3>{{name}} used by:</h3>
        <ul class="license-used-by">
          {{#each used_by}}
          <li><cite><a href="{{#if crate.repository}} {{crate.repository}} {{else}} https://crates.io/crates/{{crate.name}} {{/if}}">{{crate.name}} {{crate.version}}</a></cite></li>
          {{/each}}
        </ul>
        <details class="license">
          <summary class="{{id}}">{{name}}</summary>
          <pre class="license-text">{{text}}</pre>
        </details>
      </article>
      <hr>
      {{/each}}
    </main>
    <footer>
      <aside><strong>This may not be a complete list.</strong></aside>
    </footer>
  </body>
</html>
